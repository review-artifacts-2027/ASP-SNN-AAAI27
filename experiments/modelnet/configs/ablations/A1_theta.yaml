# A1 — Confidence threshold trade-off. ONE model per seed; the entire theta
# sweep is derived from stored margin trajectories (asp/metrics.theta_sweep).
# Logged per theta: accuracy, avg_slices, slice_utilization, exit_rate,
# risk_exited (checked against the Theorem-1 bound (C-1)(1-theta)/C),
# energy_ratio. Produces the Pareto frontier (Theorem 7 figure).
experiment: A1_theta
type: standard
eval_thetas: [0.3, 0.4, 0.5, 0.6, 0.65, 0.7, 0.75, 0.8, 0.85, 0.9, 0.95]
sweep:
  - {name: baseline}
